services:
  - type: web
    name: code-zero-web
    env: node
    plan: starter
    branch: main
    autoDeploy: true
    buildCommand: pnpm install --no-frozen-lockfile && pnpm run build
    startCommand: pnpm run start
    root: ./
    envVars:
      - key: NODE_ENV
        value: production
